name: Remove Pet Music Title Icon

on:
  push:
    branches: [main]
    paths:
      - 'scripts/remove-petmusic-title-icon.mjs'

permissions:
  contents: write

jobs:
  apply-and-verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Remove title icon
        run: node scripts/remove-petmusic-title-icon.mjs
      - name: Verify build
        run: npm run build
      - name: Commit verified change
        run: |
          if git diff --quiet; then
            echo 'No generated changes.'
            exit 0
          fi
          git config user.name 'petgrow-automation'
          git config user.email 'actions@users.noreply.github.com'
          git add src/App.jsx
          git commit -m 'style: unify Pet Music page title'
          git push origin main
