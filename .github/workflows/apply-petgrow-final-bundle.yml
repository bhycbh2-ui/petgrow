name: Apply PetGrow Final Bundle
on:
  push:
    branches: [main]
    paths:
      - 'scripts/apply-petgrow-final-bundle.py'
permissions:
  contents: write
jobs:
  apply-build-commit:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
      - run: npm ci
      - name: Apply final bundle
        run: python3 scripts/apply-petgrow-final-bundle.py
      - name: Verify server syntax
        run: node --check api/admin.js && node --check api/news.js && node --check server_lib/tarot.js
      - name: Build
        run: npm run build
      - name: Commit generated changes
        run: |
          git config user.name 'petgrow-automation'
          git config user.email 'actions@users.noreply.github.com'
          git add src/App.jsx src/PetDailyWidgets.jsx api/admin.js
          if git diff --cached --quiet; then
            echo 'No generated changes.'
            exit 0
          fi
          git commit -m 'feat: unify layout news tarot footer and analytics'
          git push origin main
